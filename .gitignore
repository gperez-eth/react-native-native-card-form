# Compiled output — built at publish time (see docs/PUBLISHING.md), never committed.
/build/

# Dependencies
node_modules/
node_modules

# npm pack artifacts (npm run check:tarball leaves one behind)
*.tgz

# Native projects generated by `expo prebuild` inside example/
example/android/
example/ios/
example/.expo/
example/node_modules/

.DS_Store
